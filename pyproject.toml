[project]
name = "urich-demo"
version = "0.1.0"
description = "Demo app for Urich: employees and tasks"
requires-python = ">=3.12"
dependencies = [
    "urich",
    "uvicorn",
    "sqlalchemy[asyncio]>=2.0",
    "asyncpg",
    "pyjwt>=2.0",
    "bcrypt>=4.0",
    "httpx>=0.27",
]
